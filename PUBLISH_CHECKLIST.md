# Publish checklist

1. `SECURITY.md`: replace `<security contact — fill before publishing>` with a real private reporting path; `LICENSE`: set the copyright line (currently "the ecosystem-starter authors") to the owner.
2. Create the GitHub repository, push `main`, enable Actions, and confirm the `ci` workflow is green.
3. Settings → mark the repository as a **template repository**.
